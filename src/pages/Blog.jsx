import * as React from 'react';
import styled from 'styled-components';
import Title from 'components/Title';

const Blog = () => {
    return(
        <Title content={"Blog"} />
    )
}

export default Blog;